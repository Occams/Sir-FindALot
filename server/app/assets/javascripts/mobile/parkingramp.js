var Parkingramp = {
  show: function(ramp) {
    console.log(ramp);
    Parkingramp.fillDetailPage(ramp);
    Parkingramp.fillMapPage(ramp);
  },
  
  fillDetailPage: function(data) {
  },
  
  fillMapPage: function(data) {
  }
}
